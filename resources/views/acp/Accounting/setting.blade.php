@extends('acp.layout.app')@section('title')    @lang('back.accounting_setting')@endsection@section('content')    <!-- Content wrapper start -->    <div class="content-wrapper">        <!-- Row starts -->        <div class="row gutters">            <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">                <div class="card">                    <div class="card-header">                        <div class="card-title">@lang('back.accounting_setting')</div>                    </div>                    @if(Session::has('msg'))                        <div class="alert alert-success">                            <strong>{!! session('msg') !!}</strong>                        </div>                    @endif                    @if($errors->any())                        <div class="alert alert-danger">                            @foreach($errors->all() as $error)                                <p>{{ $error }}</p>                            @endforeach                        </div>                    @endif                    <div class="card-body">                        <form action="{{route('setting.store')}}" method="post">                            @method('POST')                            @csrf                            <div class="row gutters">                                @foreach($array_groups as $group)                                <div class="col-xl-6 col-lg-6 col-md-6 col-sm-12">                                    <div class="card">                                        <div class="card-header">                                            <div class="card-title">@lang('back.'.$group)</div>                                        </div>                                        <div class="card-body">                                            @foreach($settings->where('group',$group) as $setting)                                            <div class="col-xl-12 col-lg col-md-12 col-sm-12 col-12">                                                <div class="form-group row">                                                    <label for="staticEmail"                                                           class="col-sm-5 col-form-label">@lang('back.'.$setting->key)</label>                                                    <div class="col-sm-7">                                                    @if($setting->input_type == 'select')                                                        <select name="{{$setting->key}}" class="form-control   selectpicker" data-live-search="true">                                                            <option value="">@lang('back.select_one')</option>                                                            @foreach($accounts as $account)                                                            <option value="{{$account->id}}" {{$account->id == $setting->value ? 'selected' : ''}} >{{$account->account_name}}, {{$account->num}}</option>                                                            @endforeach                                                        </select>                                                        @elseif($setting->input_type == 'number')                                                            <input type="{{$setting->input_type}}" name="{{$setting->key}}" id="" class="form-control  " value="{{$setting->value}}">                                                        @endif                                                    </div>                                                </div>                                            </div>                                            @endforeach                                        </div>                                    </div>                                </div>                                @endforeach                            </div>                            <div class="custom-btn-group">                                <!-- Buttons -->                                <button type="submit" class="btn btn-primary">@lang('back.submit')</button>                            </div>                        </form>                    </div>                </div>            </div>        </div>        <!-- Row end -->    </div>    <!-- Content wrapper end -->@endsection